import Link from 'next/link'; import {ArrowRight} from 'lucide-react'; import {getUser,ratingClass} from '@/lib/data'; import {AutoRefresh} from '../components_client';
export default async function Matches(){const {supabase,user}=await getUser();if(!user)return null;const {data:matches}=await supabase.from('matches').select('*').eq('user_id',user.id).order('match_date',{ascending:false});return <div><AutoRefresh seconds={20}/><header className="topbar"><div><p className="eyebrow">PARTITE</p><h1>Calendario</h1><p className="muted">Clicca una partita per vedere titolari, panchina, statistiche e voti.</p></div></header><section className="panel"><div className="match-list big">{(matches||[]).map(m=>{const win=m.juve_score>m.opponent_score,draw=m.juve_score===m.opponent_score,cls=m.status==='completed'?(win?'result-win':draw?'result-draw':'result-loss'):m.status==='live'?'result-live':'';return <Link href={`/matches/${m.id}`} className={`match-row match-row-large ${cls}`} key={m.id}><div><span className="competition">{m.status==='live'?'LIVE':m.competition}</span><div className="team-line"><img src="https://a.espncdn.com/i/teamlogos/soccer/500/111.png"/><strong>Juventus</strong><b>{m.juve_score??'—'}</b></div><div className="team-line"><img src={m.opponent_logo_url}/><strong>{m.opponent}</strong><b>{m.opponent_score??'—'}</b></div><span>{new Date(m.match_date).toLocaleString('it-IT',{dateStyle:'medium',timeStyle:'short'})}</span></div><div>{m.team_rating?<span className={`rating ${ratingClass(Number(m.team_rating))}`}>{Number(m.team_rating).toFixed(1)}</span>:<span className="pending">Da valutare</span>}</div><ArrowRight size={17}/></Link>})}</div></section></div>}
