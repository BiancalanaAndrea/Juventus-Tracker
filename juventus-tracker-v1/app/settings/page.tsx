'use client';
import {useEffect,useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function Settings(){const [season,setSeason]=useState('2026/27');const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);const client=createClient();useEffect(()=>{(async()=>{const {data:{user}}=await client.auth.getUser();if(!user)return;const {data}=await client.from('tracker_settings').select('season,provider').eq('user_id',user.id).maybeSingle();if(data)setSeason(data.season||'2026/27')})()},[]);async function save(){const {data:{user}}=await client.auth.getUser();if(!user)return;const {error}=await client.from('tracker_settings').upsert({user_id:user.id,provider:'espn',season,updated_at:new Date().toISOString()},{onConflict:'user_id'});setMsg(error?error.message:'Impostazioni salvate. Provider: ESPN.')}async function sync(){setBusy(true);setMsg('Sincronizzazione ESPN in corso…');const r=await fetch('/api/sync',{method:'POST'});const j=await r.json();setMsg(r.ok?`Sincronizzazione completata: ${j.savedMatches} partite, ${j.detailedMatches} referti aggiornati.`:(j.error||'Errore'));setBusy(false)}async function signOut(){await client.auth.signOut();location.href='/auth/login'}return <div><header className="topbar"><div><p className="eyebrow">IMPOSTAZIONI</p><h1>Gestione</h1><p className="muted">Provider, stagione e sincronizzazione.</p></div><button className="filter" onClick={signOut}>Esci</button></header><section className="panel settings-panel"><label>Provider<select className="select" value="espn" disabled><option>ESPN</option></select></label><label>Stagione<input className="input" value={season} onChange={e=>setSeason(e.target.value)}/></label><div className="settings-actions"><button className="primary-btn" onClick={save}>Salva</button><button className="filter" onClick={sync} disabled={busy}>{busy?'Sincronizzazione…':'Sincronizza ESPN'}</button></div>{msg&&<p className={msg.includes('completata')||msg.includes('salvate')?'success':'error'}>{msg}</p>}<p className="muted settings-note">ESPN viene utilizzato senza API key. Il tracker salva i dati in Supabase e recupera calendario, risultati e referti quando disponibili.</p></section></div>}
